before_script:
  - export MAKEFLAGS="-j2"

test:dev: &test
  image: sagemath/sagemath-dev:develop
  script:
    - sudo apt update
    - sudo apt install texlive texlive-latex-extra texlive-luatex -qy
    - sudo apt install graphviz imagemagick pdf2svg -y
    - sudo sed -i '/PDF/s/none/read|write/' /etc/ImageMagick-6/policy.xml
    - sage -i dot2tex
    - sage -pip install .
    - sage -c 'import sage.env; print(sage.env.SAGE_VERSION)'
    - dot -V
    - lualatex --version
    - pdflatex --version
    - convert --version
    - export DIRECTORY=`pwd`
    - cd
    - sage -t --force-lib --long ${DIRECTORY}


#- sage -c "print(sage.features.Executable(name='pdflatex',executable='pdflatex').is_present())"
#- make testdemos
#- make doc
#- make doc-pdf

# les images qui sont sur https://hub.docker.com/r/sagemath/sagemath-dev/
test:9.1.rc5-py3:
  << : *test
  image: sagemath/sagemath-dev:9.1.rc5-py3

test:9.1.rc5:
  << : *test
  image: sagemath/sagemath-dev:9.1.rc5

test:9.0:
  << : *test
  image: sagemath/sagemath-dev:9.0

# NOTE: 8.9-dev image is absent or misconfigured in docker hub...
# We just disable it for now
#test:8.9:
#  << : *test
#  image: sagemath/sagemath-dev:8.9

test:8.8:
  << : *test
  image: sagemath/sagemath-dev:8.8

test:8.7:
  << : *test
  image: sagemath/sagemath-dev:8.7

test:8.6:
  << : *test
  image: sagemath/sagemath-dev:8.6

